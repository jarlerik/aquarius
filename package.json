{
  "name": "aquarius sport club web client",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd client && npm run build && cd .. && cp -a client/build/. public/",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}